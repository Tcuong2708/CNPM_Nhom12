@model QuanLyKhachSan.Models.DichVu

@{
    ViewBag.Title = "Thêm Dịch Vụ";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<style>
    /* --- BẢNG MÀU --- */
    :root {
        --color-navy: #0f172a;
        --color-gold: #d4af37;
        --color-gold-hover: #b4932a;
    }

    /* Text & Background colors */
    .text-navy {
        color: var(--color-navy) !important;
    }

    .bg-navy {
        background-color: var(--color-navy) !important;
        color: white;
    }

    /* Nút bấm Vàng Gold */
    .btn-gold {
        background-color: var(--color-gold);
        color: white;
        border: none;
        transition: all 0.3s ease;
    }

        .btn-gold:hover {
            background-color: var(--color-gold-hover);
            color: white;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transform: translateY(-1px);
        }

    /* Tùy chỉnh ô nhập liệu (Form Control) */
    .form-control:focus {
        border-color: var(--color-gold); /* Viền vàng khi nhập */
        box-shadow: 0 0 0 0.25rem rgba(212, 175, 55, 0.25); /* Highlight vàng nhạt */
    }

    .form-label {
        font-size: 0.95rem;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-0 shadow-lg">
                <div class="card-header bg-navy text-white py-3">
                    <h4 class="mb-0 fw-bold text-uppercase fs-5">
                        <i class="bi bi-plus-lg me-2"></i>Thêm Dịch Vụ Mới
                    </h4>
                </div>

                <div class="card-body p-5">
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="mb-4">
                            <label class="form-label fw-bold text-navy">Tên dịch vụ <span class="text-danger">*</span></label>
                            @Html.EditorFor(model => model.TenDV, new { htmlAttributes = new { @class = "form-control form-control-lg", placeholder = "Ví dụ: Ăn sáng, Giặt ủi..." } })
                            @Html.ValidationMessageFor(model => model.TenDV, "", new { @class = "text-danger small mt-1" })
                        </div>

                        <div class="row mb-4">
                            <div class="col-md-6 mb-3 mb-md-0">
                                <label class="form-label fw-bold text-navy">Giá tiền (VNĐ) <span class="text-danger">*</span></label>
                                <div class="input-group">
                                    @Html.EditorFor(model => model.GiaTien, new { htmlAttributes = new { @class = "form-control", type = "number", min = "0", placeholder = "0" } })
                                    <span class="input-group-text bg-light text-secondary">VNĐ</span>
                                </div>
                                @Html.ValidationMessageFor(model => model.GiaTien, "", new { @class = "text-danger small mt-1" })
                            </div>

                            <div class="col-md-6">
                                <label class="form-label fw-bold text-navy">Đơn vị tính <span class="text-danger">*</span></label>
                                @Html.EditorFor(model => model.DonVi, new { htmlAttributes = new { @class = "form-control", placeholder = "Lần, Suất, Kg..." } })
                                @Html.ValidationMessageFor(model => model.DonVi, "", new { @class = "text-danger small mt-1" })
                            </div>
                        </div>

                        <div class="d-flex justify-content-end gap-2 border-top pt-4 mt-3">
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary px-4 fw-bold">
                                <i class="bi bi-arrow-left me-2"></i>Quay lại
                            </a>
                            <button type="submit" class="btn btn-gold px-4 fw-bold">
                                <i class="bi bi-save me-2"></i>Lưu Dịch Vụ
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>